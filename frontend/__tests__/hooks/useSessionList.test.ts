// useSessionList (localStorage-backed) has been removed.
// Session list is now managed server-side via useSessionsList (SWR on /api/v1/sessions).
// See __tests__/hooks/useSessionsList.test.ts for the replacement tests.
export {}
