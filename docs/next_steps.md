┌──────┬──────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────┐
  │ Rank │             Item             │                                             What It Is                                              │                                       Why It Helps                                        │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │                              │ A new phase between validation and the transform loop. Classifies each failing rule as: fixable via │ The transform loop currently attempts to fix rules that cannot be fixed by transforming   │
  │ 1    │ Rule Triage / Amendment Loop │  transform, threshold too strict, or fundamentally unfixable. Proposes threshold amendments. Human  │ data. This is the root cause of stagnation. Triage eliminates that class of wasted        │
  │      │                              │ approves before any transforms run.                                                                 │ iterations entirely and surfaces rule quality issues the profiler may have missed.        │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │                              │ Encode 6 tiers — structural → dedup → invalidation → filtering/outliers → standardization →         │ Makes an entire class of ordering error architecturally impossible. Imputation after      │
  │ 2    │ Canonical Transform Tier     │ imputation — as a system constant. Agent decides what within each tier. System enforces when.       │ outlier removal, null_invalid before impute_mode — these are correct by construction, not │
  │      │ Ordering (structural)        │ Custom transforms get classified into their tier at plan time.                                      │  by agent discipline. Prevents the null_invalid → not_null conflict you identified        │
  │      │                              │                                                                                                     │ directly.                                                                                 │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Holistic Transform Planning  │ Replace one-at-a-time transform suggestions with a single dependency-aware plan built across all    │ Eliminates whack-a-mole transforms. Engineer approves one coherent plan instead of 17     │
  │ 3    │ (plan before execute)        │ fixable failures. Human approves the full plan once rather than each step individually. Includes    │ micro-decisions made without context of what comes next. The advisor reasons about        │
  │      │                              │ conflicts_with and depends_on annotations per step.                                                 │ interactions between transforms rather than solving each failure in isolation.            │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Give Transformation Advisor  │ Same explorer tools as the profiler (run_sql, get_value_counts, get_sample_rows, etc.) so the       │ Currently the advisor guesses from summary statistics. With tools it can verify: "does    │
  │ 4    │ Tool Access                  │ advisor can verify hypotheses before committing to a suggestion.                                    │ winsorizing at p99 actually fix the range rule? how many rows are affected?" Matters most │
  │      │                              │                                                                                                     │  for custom transforms where the right pandas code depends on the actual data shape.      │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Pass transformation_queue to │ The validation analyzer already builds a prioritized transform queue. It is stored in               │ The validation analyzer did work prioritizing transforms that the advisor then ignores    │
  │ 5    │  the Advisor                 │ self.transformation_queue but never passed to suggest_next_transformation_activity. One argument    │ and re-derives from scratch. Free improvement — the reasoning is already done, it just    │
  │      │                              │ added to the activity call.                                                                         │ isn't being used.                                                                         │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Pre-Execution Sample         │ Before executing the approved plan on the full dataset, run the complete ordered plan on a          │ Catches plan-level problems before touching the full DuckDB session. No rollback needed,  │
  │ 6    │ Simulation                   │ stratified 1,000-row sample and verify all rules pass on that sample.                               │ no corrupted state. Builds engineer trust in the plan approval model — they can see "this │
  │      │                              │                                                                                                     │  plan was verified on a sample" before approving.                                         │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Give Validation Analyzer     │ The validation analyzer currently makes 3 sequential single-shot calls with no tools. Adding the    │ Currently the validation summary is generated from statistics only. With tools it could   │
  │ 7    │ Tool Access                  │ same explorer tools would let it investigate why rules are failing, not just that they are failing. │ look at actual failing rows, check if failures cluster around specific values or date     │
  │      │                              │                                                                                                     │ ranges, and produce a more actionable analysis. Better input to the triage phase.         │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Functional Dependency /      │ A DuckDB-based scan before rule generation that detects pairwise correlations and functional        │ These are the highest-value DQ rules — they encode actual business logic — but they       │
  │ 8    │ Cross-Column Discovery       │ dependencies — e.g., TotalCharges ≈ tenure × MonthlyCharges. Feeds findings into the profiler's     │ require multivariate analysis the current profiler doesn't do. Without this, cross-column │
  │      │                              │ investigation phase.                                                                                │  rules only get generated if the investigation happens to notice the relationship.        │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │                              │ A pre-profile pass that regex-classifies columns into semantic types: email, phone, ZIP, currency,  │ Forces the profiler to propose the right regex patterns and range bounds rather than      │
  │ 9    │ Semantic Type Detection      │ ISO date string, country code, URL. Results fed to the rule proposer.                               │ guessing from column names and sample values. Removes a significant source of rule        │
  │      │                              │                                                                                                     │ proposal noise, especially for structured text columns.                                   │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │                              │ After the triage phase, a new workflow signal approve_rule_amendments carries accepted threshold    │ The transform loop should begin with a clean, accurate baseline score reflecting the      │
  │ 10   │ Re-Validation After Rule     │ changes and rule removals. Validation re-runs with the amended rules before the transform loop      │ rules the team actually agrees with. Without re-validation, the baseline score and        │
  │      │ Amendments                   │ starts.                                                                                             │ failing rule list entering the transform loop are based on rules that have already been   │
  │      │                              │                                                                                                     │ amended.                                                                                  │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Divergence Detection During  │ During plan execution, after each transform, compare actual score delta to predicted score delta.   │ Regression detection (already built) catches rules that flip from pass to fail.           │
  │ 11   │ Execution                    │ If divergence exceeds a threshold, pause and surface to the engineer before continuing.             │ Divergence detection catches subtler cases where a transform produced less improvement    │
  │      │                              │                                                                                                     │ than predicted, indicating the plan's downstream steps may be based on wrong assumptions. │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Post-Transform Distribution  │ After full plan execution, a lightweight SQL pass checking whether any column's statistical shape   │ Catches unexpected side effects of custom transforms that didn't trigger rule regressions │
  │ 12   │ Check                        │ changed in ways the rules don't capture — new modes, shifted mean, unexpected null patterns. Not a  │  but changed the data in meaningful ways. Gives the engineer a signal that a new session  │
  │      │                              │ full re-profile.                                                                                    │ on the cleaned data might surface additional issues.                                      │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Custom Transform Tier        │ When a custom transform appears in the plan, the agent classifies it into the correct tier based on │ Custom transforms currently sit outside the canonical ordering system. Without            │
  │ 13   │ Classification               │  its nature: creates nulls → Tier 3 (invalidation), removes rows → Tier 4 (filtering), fills        │ classification they get placed arbitrarily. Correct tier placement ensures custom         │
  │      │                              │ missing values → Tier 6 (imputation).                                                               │ transforms don't subvert the ordering guarantees that prebuilt transforms enforce.        │
  ├──────┼──────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────┤
  │      │ Cross-Session Transform      │ A lightweight store of (dataset shape, column type, transform applied, score delta) accumulated     │ The advisor currently starts from zero every session. Over time, patterns emerge:         │
  │ 14   │ Memory                       │ across sessions. The advisor queries it at the start of each new session. Essentially RAG over your │ winsorizing MonthlyCharges at p99 consistently improves scores on Telco datasets. This    │
  │      │                              │  own historical runs.                                                                               │ institutional memory makes suggestions more targeted as the system accumulates sessions.  │
  └──────┴──────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────┘

  ---
  Already implemented in this conversation (not ranked, already done):

  ┌───────────────────────────────┬──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │             Item              │                                                        What Was Done                                                         │
  ├───────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Binary score formula          │ Changed category score from 1 - avg(failure_rate) to passed / total in both rule_engine.py and transformation_executor.py    │
  ├───────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Regression detection          │ Workflow now captures pre-transform passing rules and detects regressions after each transform, stored in transformation_log │
  ├───────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Regressions in advisor prompt │ Advisor now sees which transforms caused regressions with a high-priority instruction to fix them first                      │
  ├───────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Stagnation guard              │ Loop breaks after 3 consecutive applied transforms that clear zero new failures                                              │
  ├───────────────────────────────┼──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ Max iterations cap            │ Loop breaks after 25 total transform decisions                                                                               │
  └───────────────────────────────┴──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
