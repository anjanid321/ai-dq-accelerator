-- dbt model: Filter rows where {{ params.column }} {{ params.operator }} {{ params.value }}
-- Generated by AI DQ Accelerator (transformation: {{ transformation_id }})

WITH source AS (
    SELECT * FROM {{ '{{' }} ref('{{ source_table }}') {{ '}}' }}
),

filtered AS (
    SELECT *
    FROM source
    WHERE
        {% if params.operator == 'eq' %}
        {{ params.column }} = '{{ params.value }}'
        {% elif params.operator == 'ne' %}
        {{ params.column }} != '{{ params.value }}' OR {{ params.column }} IS NULL
        {% elif params.operator == 'in' %}
        {{ params.column }} IN ({{ params.value | map('string') | join(', ') | replace("'", "") }})
        {% elif params.operator == 'not_in' %}
        ({{ params.column }} NOT IN ({{ params.value | map('string') | join(', ') | replace("'", "") }}) OR {{ params.column }} IS NULL)
        {% elif params.operator == 'lt' %}
        {{ params.column }} < {{ params.value }}
        {% elif params.operator == 'gt' %}
        {{ params.column }} > {{ params.value }}
        {% elif params.operator == 'lte' %}
        {{ params.column }} <= {{ params.value }}
        {% elif params.operator == 'gte' %}
        {{ params.column }} >= {{ params.value }}
        {% endif %}
)

SELECT * FROM filtered
